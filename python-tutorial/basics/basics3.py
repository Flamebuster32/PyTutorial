# functions
def printer():  # we created a function using def with the name printer
    print("something")  # it prints 'something' whenever we call it


printer()


def slave(n):  # this time we have made it so we have made an argument n that requires any value put into it
    print(n)


slave("owo")
slave(0)
slave(1.02)

# dictionaries
dictionary = {
    "water": "liquid",
    "number": 69,
    "lmao": 420.420
}
print(dictionary)
print(dictionary['number'])
